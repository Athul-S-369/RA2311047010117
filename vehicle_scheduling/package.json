{
  "name": "vehicle-scheduling",
  "version": "1.0.0",
  "private": true,
  "description": "Vehicle maintenance scheduler microservice (knapsack + logging middleware)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "@affordmed/logging-middleware": "file:../logging_middleware",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
